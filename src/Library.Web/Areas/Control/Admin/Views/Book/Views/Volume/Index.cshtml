@model BookVolumeListingViewModel
@{
    int counter = 0;
}



<div class="card">
    <div class="card-block">
        <h3 class="float-left">@Model.Variant.Book.Title, @Model.Variant.Format.Name</h3>
        <a asp-action="CreateBookVolume" asp-route-id="@Model.Variant.Id" class="float-right btn btn-success">Add Volume</a>
    </div>
</div>

<div class="card card-block">
    <div class="card-text">
        <table class="table table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th>Volume</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.Volumes)
        {
            <tr>
                <td>@(++counter)</td>
                <td>@item.Name</td>
                <td>
                    <a asp-action="DeleteBookVolume" asp-route-id="@item.Id" class="btn btn-danger btn-sm">delete</a>
                    <a asp-action="EditBookVolume" asp-route-id="@item.VariantId" asp-route-volumeid="@item.Id" class="btn btn-success btn-sm">edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>
    </div>
</div>